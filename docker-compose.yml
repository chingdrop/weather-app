services:
  ntfy:
    image: binwiederhier/ntfy:latest
    container_name: ntfy
    command: serve
    volumes:
      - ./ntfy/cache:/var/cache/ntfy
      - ./ntfy/etc:/etc/ntfy

  weather-app:
    build: .
    container_name: weather-app
    env_file: .env
    depends_on:
      - ntfy